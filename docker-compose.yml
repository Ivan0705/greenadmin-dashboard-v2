version: '3.8'

services:
  app:
    build: .
    container_name: greenadmin
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - greenadmin-network

networks:
  greenadmin-network:
    driver: bridge
